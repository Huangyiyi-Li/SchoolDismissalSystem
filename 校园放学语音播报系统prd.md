# 校园放学语音播报系统

技术栈 Python (PyQt6 + Socket + SQLite + pyttsx3)
---

1. 项目概述
本系统旨在通过 IC 卡刷卡触发，实现自动化、数字化的放学语音提醒。刷卡器通过 TCP 协议将卡号传至 Windows 客户端，客户端根据预设逻辑进行去重判断并进行 TTS 语音播报。

---

1. 核心功能需求
2.1 硬件通信（TCP 服务端）

- 通信模式：Windows 客户端作为 TCP Server，刷卡器作为 TCP Client。
- 多设备接入：服务端需支持同时监听并维持多个刷卡器的连接。
- 心跳检测：
  - 系统需定时接收刷卡器发送的心跳包（格式通常由硬件厂家定义）。
  - 界面需实时显示硬件的“在线/离线”状态。
  - 若超过一定时间（如 30 秒）未收到数据，状态标记为“离线”。
2.2 刷卡逻辑处理
- 数据解析：从 TCP 原始报文中提取出 IC 卡的内码（卡号）。
- 班级匹配：通过本地 SQLite 数据库查询卡号对应的班级名称。
  - 规则：支持一对多（一个班级对应多张卡）。
- 时间窗口校验：
  - 只有在设定的时间段内（如：16:30 - 18:30）刷卡才有效。
  - 非指定时间段内的刷卡，系统记录日志但不触发播报。
- 全局去重：
  - 在同一个有效时间段内，同一个班级只播报第一次。
  - 直到下一个有效时间段开启，去重机制重置。
2.3 语音播报与队列管理
- 播报内容：[班级名称]正在放学，重复三遍。
- 队列机制：
  - 当多个班级几乎同时刷卡时，系统需进入队列。
  - 先进先出 (FIFO)，播报完一个再播报下一个。
  - 播报期间不阻塞 TCP 数据的接收。
- 技术实现：使用 Python TTS 库（如 pyttsx3 或 edge-tts）实现文字转语音。

---

1. 界面需求（前端 - Python PyQt6）
虽然是后台逻辑为主，但前端需要一个监控界面：
1. 连接状态区：显示当前已连接的刷卡器 IP 地址及在线状态。
1. 实时日志区：滚动显示刷卡记录（时间 | 卡号 | 对应班级 | 是否成功播报）。
1. 配置显示区：显示当前的有效时间段（如：16:30-18:30）。
1. 待播报队列：显示当前正在排队等待播报的班级名称。

---

1. 数据结构设计 (SQLite)
表 1：卡号班级映射表 (mapping)
字段名 类型 说明
id INTEGER 自增 ID
card_id TEXT IC 卡内码 (唯一)
class_name TEXT 班级名称

表 2：播报记录日志 (logs)
id INTEGER 自增 ID
swipe_time DATETIME 刷卡时间
card_id TEXT 刷卡内码
status TEXT 状态（成功播报/时间段外/重复刷卡）

---

1. 非功能性需求

- 稳定性：TCP 服务端需具备自动重连监听能力，防止由于网络波动导致的程序崩溃。
- 并发处理：刷卡接收、逻辑处理、语音播报需分属不同线程，确保界面不卡顿。
- 配置化：时间段、播报重复次数、端口号应写在配置文件（.ini 或 .json）中。
